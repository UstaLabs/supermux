# Implementation roadmap and recovery checkpoint

The user authorized continuing through the full extraction roadmap. Do not stop after each milestone to ask to continue. Do not mark a task complete merely because an API method exists or a fixture passes. Do not use brainstorming.

No commits, live broker restarts or package publication have been performed. Prepare concrete release artifacts and migration code before any separate publication/deployment approval required by repository rules.

## Scope

- [x] Standalone core, storage, queues, events, interrupt, ACP fixture integration.
- [ ] Native Codex driver and real lifecycle verification.
- [x] Authentication transport / account isolation / refresh behavior.
- [ ] Native Claude, Cursor, OpenCode and Grok drivers with capability-specific parity.
- [ ] Fork / steer / detach / configuration / native history semantics and tests.
- [ ] Broker migration with compatibility and recovery checks.
- [ ] External-consumer docs, API compatibility and distribution preparation.
- [ ] Final independent review and validation.

## Current checkpoint (2026-09-11)

- Grok broker spawn/resume/config/kill/shutdown are migrated locally. Independent review and parent164 focused tests, four configuration probes, five lifecycle probes passed. Running broker is unchanged; no restart/deployment.
- Core last full standalone suite234pass; seven new auth tests bring coverage forward (parent19auth tests pass; author49affected tests pass). Auth review currently running.
- Full repository gate3694pass3skip1fail across504files; only existing live-provider /usage timeout. Deterministic command test fix is queued.
- Root workspace/Docker/CI install wiring landed. Fresh Core build/import passes. Fresh root install exposes transitive Bun/Node type dependencies; parent explicit type-selection probe passes, permanent manifest/config fix running /tmp/core-workspace-types-fix.log (exec66214).
- Public API docs spec review passed, quality polish queued (honest custom-driver configuration and assertions; remove workflow diary). Packed updated example passed actual Node22.
- Codex real lifecycle previously passed. Native autonomous activity/usage/steering support must land before broker migration: parent /tmp/core-codex-autonomy-parent.ts currently demonstrates the missing notifications/false idle. Bounded implementation brief prepared, not started.
- Next implementation order: finish workspace types; docs quality; deterministic usage test; Codex activity and broker integration. Remaining agents/capability limits and final review still required. One Grok4.6low implementer at a time; root verifies. Latest details and report paths are appended below.
- Progress: https://supermux-core-progress.ustalabs.com (localhost33183). No commits/publication/restart or auto-resume watchdog.

## Historical checkpoint (2026-09-06)

- Core baseline: 36 tests passed; build and typecheck passed; compiled fixture demo worked in Bun and Node 22.23.2.
- Native Codex subagent hit usage limit after creating src/codex, tests/codex.test.ts, fixture. Parent is verifying these files; do not assume them reviewed or complete.
- Auth helper under src/auth and tests/auth.test.ts: 8 tests pass. Explicit copiedCredentials + withAuth decorator. Canonical promotion requires unchanged baseline; source lock coordinates library writers, not unrelated CLI account changes. Failed auth cleanup can be retried through provider.close(). Needs independent review.
- Existing whole-repo baseline: 3421 pass, 3 skip, 2 fail. /usage timeout also fails alone; sessiond timeout passes when its whole file runs alone.
- Progress website: docs/core-progress/{index.html,data.json}, https://supermux-core-progress.ustalabs.com, localhost:33183. Docs are gitignored; source package is untracked, not committed. Keep data.json updated.
- Subagent hit provider hard limit; root continued. No auto-resume watchdog is configured. Do not claim a stopped provider can self-wake. Preserve this file when approaching context/usage limits.

## Verification commands

From repository root: bun run typecheck:core; bun run build:core; bun test packages/supermux-core/tests.
Full repository: bash .mux/verify.sh (known baseline failures above).
Read only metadata, never print credential file contents. Real smoke tests should use disposable workdirs and explicit read-only/tool-disabled policies.

### Live verification update
- Codex CLI 0.153.4: real create, model completion, native fork, confirmed interruption/cancelled completion, close and exact-identity resume passed.
- Found and fixed native interrupt race: turn/start acknowledgement precedes turn/started; wait for the latter before interrupt/steer. Early completion must also release that wait. Fixture now reproduces both cases.
- Grok 4.6 low and OpenCode: real native ACP create, model turn, close and exact resume passed.
- Claude CLI 2.1.261 stream-json driver is being written by Grok CLI subagent. Read /tmp/supermux-claude-driver.log for progress.
- Native/auth/fork independent review is running via Grok CLI, output /tmp/core-native-review.md.

### Grok 4.6 low supervised implementation checkpoint
- Per latest user instruction, implementation/review tasks now run through Grok CLI `--model grok-4.6 --reasoning-effort low`; parent verifies/steers. No default/high collaboration implementers.
- Claude real isolated create/turn/interrupt/close/exact resume passed. Inherited CLAUDE_CODE_OAUTH_TOKEN overrides copied credentials; explicit inheritEnv:false plus PATH/HOME/CLAUDE_CONFIG_DIR fixed authentication. Empty unprompted sessions may have no durable native history.
- ACP cancellation now retries within configurable time window, scoped to the exact active turn. Real immediate Grok cancellation passed. OpenCode basic flow passed, but interrupt completion reported completed rather than cancelled; do not overclaim cancellation.
- Core configure/history contracts added with persistence/rollback and explicit unsupported capabilities. Parent whole-package run: 72 pass, 0 fail, 260 assertions. Native implementations and independent config review in progress.
- Auth independent Grok 4.6 low review found no blocking issues, including source-lock retry and lease-release retry probes.
- Active new CLI tasks: /tmp/core-codex-config-low.log (Codex config/history); /tmp/core-cursor-driver-low.log (Cursor driver); /tmp/core-config-review-low.log (read-only transaction review). Reports use matching -report.md names. Cursor research: /tmp/core-cursor-research-low.md.
- Cursor print --resume does not reliably reject missing history; new driver must validate cwd-hashed store.db first. create-chat mints ID but may remove empty store; do not seed synthetic model prompts. Native Cursor not yet live-verified.

### Review fixes and migration preparation
- Parent verified configuration/core changes; initial combined run was 49 pass/1 fail because Claude permission duplicate IDs could invoke host twice. Do not claim full package green until that review fix lands and is rerun.
- Cursor fixture driver exists (not yet exported). Parent found/fixed via low agents: nonzero exit after success result, missing result identity, post-result hang, missing executable cleanup hang (spawn error has no exit event). Latest report /tmp/core-cursor-spawn-fix-low-report.md; real create-chat still times out, so native Cursor remains unverified.
- Codex configuration/history exists with explicit native default restoration or rejection when unknown. Cross-resume contaminated defaults and host command/file approvals currently being fixed by /tmp/core-codex-approval-low.log.
- Core requested configuration is cloned, persisted only after native apply, and rolled back on native or storage failure. Failed rollback marks session failed. Empty requested configuration means driver/factory defaults, not a reported native effective snapshot.
- Broker migration research /tmp/core-broker-migration-low-report.md. Parent corrections: default permission cancellation is a denial, not a deadlock; never make library always-approve default. Existing broker unattended policy may be supplied explicitly by its host. Grok vendor notifications include self-started background task turns and must survive migration; generic ACP currently drops them. Preserve replay tagging and host turn/event semantics.
- Next library prerequisite: optional caller-specified core ID and metadata-only adopt (existing native ID, no fake create), current task /tmp/core-adopt-low.log. Need Grok configuration/restart support, explicit broker policy flags, and opaque ACP extension update forwarding before broker bridge.
- Active low tasks at this checkpoint: core adopt, Codex approval/reset, Claude permission duplicate/late-answer fix. Parent Cursor final verification /tmp/core-cursor-final-parent.log and temporary missing-command/result probes.

## Consumer docs / packaging checkpoint (2026-09-08, Grok 4.6 low)

Bounded docs-only pass. Owned: README.md, API.md, examples/real-agents.mjs, examples/custom-driver.mjs, package.json Cursor export only (private/version/dependencies unchanged), this ROADMAP append. No commits, publish, deploy, live calls, credentials, or root lockfile edits.

- Cursor subpath export `./cursor` → `dist/cursor`. Other drivers already exported.
- README/API document actual imports, Node >= 22, explicit state/cwd, receipts/queue/interrupt/close/resume/fork/adopt/config/history, capability table, host vs core vs driver, env precedence (process unless inheritEnv false → factory env → profile env), no secrets in records, copied homes disable fork.
- Permissions: default deny/cancel vs explicit `permissionPrompts: "host"` and selected-once; no bypass default. Grok `noLeader` true / `alwaysApprove` false unless broker sets both explicitly.
- Requested vs effective configuration; unknown native reset may reject; Grok configure close/reopen exact native id.
- Status: Cursor fixture-only, live create-chat timeout; Claude/Codex/Grok/OpenCode basic live earlier. Latest Codex check hit model limit; history/config/fork/interrupt/resume had passed. Parent still reviewing Grok ownership/permissions — not claimed complete.
- Limits documented: no synthetic transcript fork, detach unsupported, forget is metadata not native erase, queues not durable, crash lock manual recovery.
- custom-driver example: in-process echo, mkdtemp + finally cleanup. real-agents: argv opt-in agent, caller env/home, no credential copy, default no-tools prompt.
- Verification of this pass is recorded in `/tmp/core-consumer-docs-low-report.md`.

### 2026-09-08 continuation checkpoint
- Parent adopted-record/core/config/store/auth run: 57 pass. Cursor final parent fixture suite: 11 pass. Native Claude/Codex parent suite after cache/malformed-host fixes: 40 pass. Parent corrected review probe to inspect actual decisions: Claude cross-turn reused request ID produces allow then deny; both malformed-host probes complete without runtime failure.
- NEW remaining Codex bug independently reproduced: late pending permission callback after normal turn completion dereferences `active!.id` after active cleared, causing unhandled TypeError. /tmp/probe-codex-late-approval.mjs and fixture reproduce. Needs low-agent fix: capture original active/turn/thread at request arrival, grant only if same unfinished active/controller live, handle late result without unsafe access; key IDs by JSON type as well as value.
- Latest native Codex config smoke: history/config persistence/fork/interrupt/resume/reset passed, first model completion failed provider usage limit (not a full live success). /tmp/core-live-codex-configuration.log.
- Grok migration helper has config via exact-native-ID restart, explicit alwaysApprove/noLeader options, opaque ACP extension events, initialize metadata. Review followup /tmp/core-grok-ownership-low.log correcting command suffix magic, retained child cleanup ownership, candidate-exit handling. Beware test mocks leaking into ACP suite; isolate injected child factory in tests.
- Broker bridge implementation running /tmp/core-grok-bridge-low.log, owns only new grok/core-adapter.ts + tests, NO production call-site wiring yet. Next: process-level host Core provider, preserve auth/config/shim/registry, route Grok spawn/resume/applyConfig/shutdown through bridge with regression checks.
- Consumer docs/examples/package Cursor export running /tmp/core-consumer-docs-low.log. No package publication, commits or live broker restart authorized/performed. Full-suite rerun remains after active edits settle.

### 2026-09-08 supervised verification checkpoint
- Parent full library suite: 140 pass, 0 fail, 593 assertions (/tmp/core-full-current-parent.log). Package build/typecheck and root tsc rerun, no errors reported.
- Codex late permission callback fix verified: 27 tests and no unhandled callback probe. Claude permission regression probes verified.
- Parent real Grok current-source smoke: model turn, configure medium then low through exact-native-ID restart, close/resume identity, stopped interrupt and cancelled completion all passed (/tmp/core-live-grok-config-parent.log).
- Cursor ACP real model turn works but no native store.db written; exact resume rejects Session not found. Native lazy SQLite implementation has no-op local-only writes/flush; precise causal path remains unproven. /tmp/core-cursor-persistence-research-parent.md. Do not fake native history or mark resume supported by live evidence.
- Parent coalesced ACP NDJSON probe preserves standard/vendor event order and replay flags across load response; suspected ordering issue NOT reproduced. /tmp/core-acp-order-parent.json. JSON-RPC batch is unsupported and is not evidence of a supported-format ordering bug.
- Grok bridge stop fixes underway via /tmp/core-bridge-stop-fix.log. Parent found additional resume() shortcut bypasses stopping: resume returns while close pending, subsequent send throws stopped (/tmp/core-bridge-resume-race-parent.ts). Must fix before wiring. Stop currently also emits turn-complete before confirmed cleanup; review that.
- Host review /tmp/core-host-review-next-report.md confirms stale adapter can attach to replacement session. Broker always creates new adapter on resume, so enforce terminal host-owned handle synchronously when stop begins, allow only retry stop thereafter; new createAdapter after confirmed cleanup. Standalone bridge can retain restart semantics.
- No production call-site wiring or live restart yet. Progress website refreshed with current status.

### 2026-09-08 background activity design correction
- Lifecycle fence fixed and independently approved for scoped stop/start/resume/failedclose/stalehosthandles. Parent 43 adapter+host+parser tests pass; delayedclose/resume probes now correct. /tmp/core-grok-fence-final-review-report.md. This is NOT overall migration approval.
- Confirmed NEW migration blocker: autonomous Grok vendor turn opens UI but Core remainsidle; interrupt sends zero native cancels and closesUI. Queue/config/fork may overlap autonomouswork. Parent /tmp/core-background-parent.ts + independent /tmp/core-background-parity-review-report.md. Do not disableleader orfakeprompts tohideit.
- PhaseA generic driver-nativeactivity contract running Grok4.6low /tmp/core-native-activity-contract.log, brief samebasename-brief.txt. Owns types/core/session/index +activitytests only. Optional DriverContext.onActivity withid/phase; tracksnativework separatelyfromreceipts inclownpromptoverlap; gatesqueue/config/fork andwaitsrealinterruptcompletion.
- PhaseB ACP/Grok plumbing research /tmp/core-acp-activity-plumbing-research.log, report samebasename-report.md. Need vendor classifier outsidegenericSession, replay gating, allturns tracking, cancelretries boundactivitygeneration, no latecompletionclosingnextturn.
- Bridge must deriveUIbusy/idle fromcorestate oncehonest. Current onCoreEvent(message.completed) still cancloseactiveUI fordiscardedqueuedreceipt duringinterrupt, andrawturn_completed unbound. Stopfix guardsreceiptawait+startepoch but notthisinterrupt eventpath. StageC musttest.
- Initialconfiguration mustapplybefore nativeleaderopen: currentbridge create thenconfigure canraceautonomouswork. Adopt alreadyacceptsconfiguration; typedCreateOptionsconfiguration extension likelyneeded. Productionwiring brief prepared /tmp/core-grok-production-wiring-brief.txt, NOT launched untilactivityprerequisitespass; addinitialconfiguration requirements.
- Packagepolish completed: API.md/examples packed, vendorcredential/historysideeffectsaccurate, typesNodeNext with explicitnode ambient documented. Parent packedNode22echo passed (/tmp/core-package-polish/external-consumer).
- Codexbrokernextresearch /tmp/core-codex-broker-next-research-report.md. Parent correction: core.send(queue) serializes nextprompt (does NOT itself overlap/promptbusythrow); stilllosesoldbrokersteersemantics, so Codexbridge muststeeractiveinputs. Usageaccount/rateLimits andspontaneousnativeevents currentlyfilteredout bycoredriver; nativeimageconversion/hostMCPauth/ownership needed. Keeptheseparatefuturetask.

### 2026-09-09 activity implementation review checkpoint
- PhaseA optional ActivityNotice {id,phase:'started'|'completed'} implemented in types/core/session/index +tests/activity.test.ts. Agent reports152fulltests; parent targeted12pass47assertions. NOT approved: parent /tmp/core-activity-close-parent.ts confirms interrupt returns stopped after failednativeclose and afterruntimefailure (statefailed) because releaseActivityWaiters resolvesgatesprematurely. Coreopenbuffer cappedraw256 notices dropsfinalactivity; outstandingcap256 silentlydropswork. Independentreview /tmp/core-activity-contract-review.log/report.md inprogress.
- Need PhaseAfix: terminalclosing/failure reject/supersede interrupt ratherthanmanufacturingcompletion; activitygates onlysuccessonmatchingend orconfirmedcleanup withhonestresult. Compactbufferoutstandingbyid andfailonoverflowinsteadofsilentlylosingwork; validate/copy notices. No blanketcompletednotificationsonEOF.
- PhaseB ACP/Grok implementation now running /tmp/core-acp-native-activity.log; brief /tmp/core-acp-native-activity-brief.txt. Ownsacp/index,agents/index andtests only. Optional generic classifier suppliedbyGrok; allnativeactivity separatefromownedprompt, replayignored, processuniqueids, generationboundcancelretries andwrappercandidatecallbackguard. Do notchangePhaseAinterfacewhileBactive.
- Future PhaseC bridge UI shoulduseorderedcorestateChanged busy/idle, notrawcompletion ormessage.completed. Currentdiscardedqueuedreceipt cancloseactiveUI duringinterrupt throughonCoreEvent(message.completed) despiteawaitreceipt epochguard; stategroundtruthfixrequired.
- Initialconfiguration beforeleaderopen required: typedCreateOptions.configuration+validation, passedbybridgecreate/adopt; avoidpost-open defaultmodelrace. Existingstoredcoreconfig vsbrokerdesiredconfiguration onresume needs explicitpolicy, no silentnew.
- Grokproductionwiringbrief preparedbutNOTlaunched. Nativeactivityprerequisitesblock it. UsercontinueSep9 preservesautonomousworkandGrok4.6lowagentmandate.

### 2026-09-09 parent activity verification update
- Core activity failure semantics fixed and independently approved: 80 focused tests passed. Failed cleanup/runtime exit rejects pending interrupt confirmation; no fabricated stopped outcome.
- Parent reran ACP/agents/activity/integration after identity fix: 64 pass, 0 fail, 266 assertions. Coalesced own-A response does not complete background-B; real Grok no-ID start + UUID completion shape returns idle.
- Separate identity review still running; parent flagged unknown explicit completion changing classifier matching mode for verification.
- Permission lifetime fix running via Grok4.6low: /tmp/core-acp-permission-fix.log; actual late autonomous approvals after cancel/end were reproduced. Candidate ownership/bounded buffer brief prepared /tmp/core-grok-candidate-fix-brief.txt, not launched yet.
- Initial configuration and broker UI core-state integration remain before production Grok wiring. Site refreshed; full package baseline remains 140 tests from before activity changes, not a claim current full tree is verified.

### 2026-09-09 ACP review closure and next fixes
- Parent current ACP/agents/activity/integration: 72pass, 0fail, 284 assertions (/tmp/core-acp-final-parent.log). Delayed-start permissions stay cancelled; ended A approval aborts while unique B can allow; cancelled outstanding A+B denies ambiguous requests; explicit A ignores unknown B/C/no-ID completions until matching end.
- Independent Grok4.6low review /tmp/core-acp-activity-final-review-report.md: scoped spec passes, no blockers. Overflow background close needs rejection handler (quality fix queued with candidate task). Sequential no-ID Grok pairing and bounded64-ID dedupe limitations documented; not arbitrary ordering protection.
- Grok candidate ownership/buffer fix now running /tmp/core-grok-candidate-fix.log. Parent /tmp/core-grok-candidate-cleanup-parent.ts confirms failed candidate gets one failedclose only; Core.close falsely resolves without retry before fix.
- New auth forwarding defect parent reproduced and independent review confirmed: withAuth advertises configure/history but drops configure/configuration/history methods. /tmp/core-auth-capabilities-parent.ts and /tmp/core-auth-forwarding-review-report.md. Fix brief prepared /tmp/core-auth-forwarding-fix-brief.txt, not launched yet.
- Pre-open config design reviewed /tmp/core-config-preopen-review-report.md. Shared validation/merge, typed create config, explicit resume overrides before native open, no-option-only resume coalescing, restore original record after failed override. Brief /tmp/core-before-open-config-brief.txt. Broker UI/stall design note /tmp/core-broker-activity-bridge-design-note.md.
- Production Grok wiring still pending all prerequisites; no live restart/deploy/commit/publication.

### 2026-09-09 Grok cleanup ownership approved
- Candidate wrapper now retains unpublished runtimes until confirmed close; initial failed cleanup returns failed close-only facade via onExit so Core owns retries. Configure cannot replace until cleanup succeeds. Accepted native ID survives rejected candidate (including public getter), setup+cleanup errors both retained.
- Activity copied/validated and compact bounded at256 outstanding IDs before/after publication. Failed candidate notices never published. ACP overflow background close catches rejection; explicit close still propagates cleanup failure. ACP TypeScript narrowing corrected.
- Parent agents/activity/core: 74pass0fail295 assertions (/tmp/core-grok-candidate-final-parent.log); package typecheck passes. Parent initial/configrollback/public-ID probes pass. Independent /tmp/core-grok-candidate-final-review-report.md: no scoped blockers. Wrapper open throwing before child returns runtime remains child-driver responsibility.
- Auth forwarding fix NOW running /tmp/core-auth-forwarding-fix.log (Grok4.6low). Next initial config API, then bridge ordered core state/message.started watchdog, then production Grok wiring. Full package rerun pending auth/config changes. No deployment/restart/commit/publication.

### 2026-09-09 auth forwarding approved / full suite current
- withAuth optional configure/configuration/history now forwarded with inner receiver; unsupported methods absent, copied-home fork disabled, credential lifetime unchanged. Independent low review no blockers (/tmp/core-auth-forwarding-final-review-report.md).
- Parent /tmp/core-auth-integration-parent.ts verifies Session.configure/history/close/resume through withAuth; requested model persists and native close precedes lease release.
- Parent FULL current library:196pass0fail794assertions across12files (/tmp/core-full-after-auth-parent.log). This replaces old140-test baseline. Package typecheck passes after ACP narrowing fix.
- Pre-open configuration API task now running /tmp/core-before-open-config.log, brief same base-brief.txt, Grok4.6low34turns. Planned optional create config and resume overrides with original-record rollback/concurrency rules. Treat configuration:undefined as omitted; {} explicit patch; undefined patch values clear keys.
- After config: /tmp/core-broker-activity-bridge-brief.txt (newmessage.started + orderedcorestate + watchdog + startupsettings), then productionwiringbrief. Parent corrections to low bridge review included in designnote: no raw-start secondary latch; observer ordering is not physical-before-prompt; don'tmiss message.started before send-await storesreceiptid; watchdog must restore continuation after confirmedinterrupt.

### 2026-09-10 pre-open config verified / failed-open identity ownership gap
- Pre-open create/resume configuration implemented; partial-write rollback defect found by parent and fixed via persistenceAttempted flag. Parent config/core/adopt/activity:89pass0fail337assertions. Both parent fault modes oldconfig preserved/nativeclosed. /tmp/core-before-open-config-fix-parent-report.md; independent /tmp/core-preopen-config-review-report.md no novelblockers (stale WIP test note superseded by89pass).
- NEW parent actual /tmp/core-failed-open-id-reuse-parent.ts: after driver onExit duringopen + runtimeclosefailed, Core keeps only globalcleanupSet, sameIDcreate canopen native2 before native1 closes. Bridge failedstart has noSessionhandle; stop mayunregister hostID/env withoutreaping Corependingcleanup. Need by-ID cleanup and ownership gate beforeproduction.
- Read-only Grok4.6low research running /tmp/core-failed-open-reuse-research.log; possible sessions.close(id) buildingblock, no API decision yet.
- Independent implementation running /tmp/core-message-started.log (12turns) onlytypes/session/newmessage-events tests/API: dispatchstarted event needed for watchdog; no fakeautonomousreceipt. Bridgebigbrief will use event oncepassed ratherthanreimplement.
- No productioncallsitechanges/restart/deploy/commit/publication. Last parent fullpackage196pass predatespreopenconfig; newer parentfocused89pass. Initialpreopenagentreported213pass beforepartialwritefix; not parentfullclaim.

### 2026-09-10 close-by-ID approved / current full library verification
- Core.sessions.close(id) now waits for claimed create/adopt/resume/fork and retries retained native cleanup by ID. Failed cleanup blocks identity reuse until confirmed close. Guards remain active through the close promise finalizer. Parent immediate-create, reentrant-fork and microtask-offset probes pass. Independent close review approves; minor post-shutdown closePending retention is harmless and remains cleanup polish.
- Parent FULL package: 234 pass, 0 fail, 945 assertions, 14 files (/tmp/core-full-close-parent.log). Includes pre-open configuration rollback, message.started and close-by-ID tests.
- Grok4.6low broker bridge implementation running /tmp/core-broker-activity-bridge.log, exec3896; ordered state authority, dispatch watchdog, pre-open settings and stop via close(id). Public API documentation read-only audit /tmp/core-public-api-doc-audit.log, exec25228.
- Production wiring still pending bridge verification. Config queue design report /tmp/core-grok-config-queue-research-report.md; parent steering warns against recursive busy microtask retries and losing earliest rollback baseline during in-flight newer changes. No production call-site changes, commits, publication or restart.

### 2026-09-10 bridge state approved / host shutdown ordering fix
- Grok bridge now uses ordered Core state as UI latch authority, message.started watchdog, pre-open config and close(id) cleanup. Parent75affectedtests256assertions + root/package typecheck pass. Parent attachment-admission and failed-open-host-retry probes pass. Unconfirmed watchdog focused test passes after stronger timing assertion. Independent /tmp/core-broker-activity-final-review-report.md confirms scoped bridge state spec met,80tests278assertions.
- NEW demonstrated host shutdown blocker: shutdown awaits adapter.stop before core.close triggers lifetime abort; open waiting ctx.signal never exits. Parent /tmp/core-host-shutdown-open-parent.ts returns aborted:false/closed:false until manual release. Fix via Grok4.6low /tmp/core-host-shutdown-fix.log exec96830,18turns; ownshost+hosttests. Parent report /tmp/core-broker-activity-bridge-report.md captures main/finish CLI turnlimit checkpoints (source landed despite missing agentreport).
- Public API docs audit /tmp/core-public-api-doc-audit-report.md; prepared fix brief /tmp/core-public-api-doc-fix-brief.txt NOT launched (one implementation agent at a time). Parent corrections included adopt no-spawn, shutdown close join nuance, runtime.configuration not universally native snapshot.
- Codex native activity design CLI hit14turns withoutreport; parent /tmp/core-codex-migration-parent-notes.md records concrete gaps, including native-only steer blocked by Session.active requirement. Do not claim extra design approval.
- After host fix verification, production Grok wiring brief+steering ready; no production call sites changed yet.

### 2026-09-10 host shutdown approved / production Grok migration started
- Host shutdown now fences handles then runs core.close before adapter.stop; clear registrations only after native cleanup success. Parent abort probe true/true, failed-open retry probe still correct;59tests225assertions pass. Agent root/package tsc passed. Independent /tmp/core-host-shutdown-review-report.md specpass/qualityaccept, source review no novelblocker.
- Production Grok wiring NOW running Grok4.6low50turns /tmp/core-grok-production-wiring.log, exec35719. Brief +steering samebasename; owns dialect/provider/manager/runtime/spawn-helper/main shutdown/typespendingconfighelpers/tests. Must write incremental report, preserve typed-busy pending settings/earliestbaseline/no recursive microtask retries, awaited kill, auth/config/registry-beforestart. No deployment/restart/commit.

### 2026-09-10 packed Node consumer verified / production draft review
- Fresh build+tarball /tmp/core-package-sep10/supermux-core-0.0.0.tgz installed into separate /tmp/core-package-sep10/consumer. Parent Node22.23.2 check.mjs passes all7publicexports, pre-open settings, native activity queue/no early started, message.started, close-by-ID and exact resume/config clear. Docs follow-up remains prepared, so artifact is validation snapshot, not final release.
- Production Grok agent exec35719 still running. Actual manager+Core probe /tmp/core-manager-config-race-parent.ts shows second model remains queued forever after held firstconfigure (registry grok-4.5 vs adapter grok-4-fast); agent steered to in-flight apply serialization and baseline retention. Actual provider probe /tmp/core-provider-cleanup-retry-parent.ts shows close loses owner before cleanup confirms (2closecalls only1attempt); agent must retain host on failure and test retry.
- Auth coverage read-only audit /tmp/core-auth-coverage-audit-report.md complete; prepared /tmp/core-auth-coverage-finish-brief.txt (not launched). Focus uncovered lock/failedrelease/profileerror contracts, no realtokenfiles/loginUI.

### 2026-09-10 production wiring landed, NOT yet accepted
- Grok4.6low production agent finished, report /tmp/core-grok-production-wiring-report.md. Root60tests0fail156assertions5files --isolate pass;git diff --check clean. Actual call sites now changed in worktree (no deploy/restart).
- Normal modelM1/M2 serialized probe and provider close-retry probe now pass. NEW parent /tmp/core-manager-config-failure-race-parent.ts loses newer M2 when M1 fails yet reportsM2applied; /tmp/core-manager-config-new-pending-parent.ts drops M2pending on M1success when brokerphasebecomesbusy. Manager-only fix running /tmp/core-manager-config-races-fix.log exec14100 Grok4.6low26turns, ownsmanager/pendinghelpers/tests. Must preserve revisions and native-consistent rollback baseline; unconditional pending.take/registryrollback are wrong across newerrequests.
- Production dialect OwnedSlot.ready=false still conflates healthyinflightstart with failedcleanup, retrycleanup occursafterauth/configwrites. Read-only review /tmp/core-grok-start-admission-review.log exec49971 Grok4.6low12turns. Next bounded dialectfix likelyneeded; do not close healthyinflightstartup as failedcleanup.
- Package externalNode22 passed currenttarball; docsfollowup andauthcontractcoverage briefs readybutnotlaunched. Production broadreview, Codexmigration/nativeactivity and finalgates still outstanding.

### 2026-09-10 startup admission race reproduced
- Read-only startup admission CLI hit12turns withoutreport, but wrote /tmp/core-grok-start-admission-probe.ts. Parent added rejection handlers and ran it: concurrent resume yields open1/close1/first:ok/open2/second:ok; healthy first startup silently stopped, both callers fulfilled. Later stop of first cannot stop replacement; nextresume already-live.
- Prepared NOT launched /tmp/core-grok-start-admission-fix-brief.txt (24turns planned) and steering. Ownsdialect+tests; explicit starting vs failed-cleanup phases, admission before auth/config writes, cleanup retry first, exact handle identity and healthy post-stop reuse. Launch after current manager-only fix completes.
- Manager config fix exec14100 inprogress; parent flagged success baseline wrongly reads latestdesired registry afterawait instead of attempted native configuration. Steering same productionmailbox.

### 2026-09-10 configuration race fix verified / startup correction running
- Manager revision-aware config fix complete via Grok4.6low; parent ALL4actual manager+Core probes pass: M1/M2 normal, M1failure/newerM2, newerpending whilebusy survivesM1success, M2failure afterM1success rolls actualnative+registry+adapter toM1. Parent52manager/pendingtests136assertions pass; agent roottsc clean. /tmp/core-manager-config-races-fix-report.md. Independentread-only review /tmp/core-manager-config-races-review.log exec93047 inprogress12turns.
- Startup admission/recovery correction NOW running /tmp/core-grok-start-admission-fix.log exec belowtoolreturn (Grok4.6low24turns). Brief+steering prepared; ownsGrokdialect+tests only, explicit state/early admission beforeprivatehome writes. No productionapprovalyet, no deploy/restart/commit. Latestusercontinue preservesfullobjective.

### 2026-09-10 stale stop handle regression found during admission fix
- Manager config independent review /tmp/core-manager-config-races-review-report.md ACCEPT; all4parentprobes +52tests alreadypassed.
- Startup admission fix exec98243 running. NEW parent /tmp/core-host-stale-stop-parent.ts: oldhandle.stop succeeds, replacement starts, oldhandle.stop AGAIN closes native2; replacement.send errors Session is closed. By-ID close introduced this stale stop regression. Parent authorized minimal core-host.ts/test scope extension via /tmp/core-grok-start-admission-fix-steering.md: stop idempotent after confirmedrelease, retryallowed onlyuntilcleanupsuccess, flagsetbeforeunregister. Preserveglobalshutdownteardown andconcurrentstopjoin.

### 2026-09-10 startup finish checkpoint
- Startup main24turn agent hitlimit; healthy concurrent admission probe now PASS, reportstillcheckpoint. Root additional /tmp/core-grok-register-failure-parent.ts: registry.register throws after adapterallocation, retry sameid blocked alreadystarting despiteopens0.
- Finishagent NOW /tmp/core-grok-start-admission-finish.log exec95402 Grok4.6low20turns. Ownssession+host/tests: staleoldstopidempotency, synchronouslyreservefailedcleanupretry, registry/preallocationfailurecleanup. Brief samebasename; oldsteering containsactualprobes. No broadproductionacceptanceyet.

### 2026-09-10 Grok focused migration verification / clean-install integration
- Tiny recovering-slot fix complete /tmp/core-grok-recovering-slot-fix-report.md; preserve recovering token in dropOwned until reserveAdmission transitions. Root all5lifecycleprobes PASS: dialectpersistentfailedcleanupretry, staleoldstop, registryfailure retry, concurrenthealthyadmission/reuse, hostabortstartup. Root164tests0fail488assertions11files Grokfolder+manager+pending+spawnhelper --isolate (/tmp/core-grok-migration-current-parent.log).
- Final scoped startup review /tmp/core-grok-admission-final-review.log exec42304 running12turns. Managerreview accepted; all4configprobes passed.
- Clean-install audit /tmp/core-workspace-install-audit-report.md confirmedrootfrozeninstall missesSDK, copyingcoremanifestwithoutworkspace stillmissing, workspace registration resolvesnestedSDK; Dockerordercurrentlybroken. Implementation NOW /tmp/core-workspace-install-fix.log exec followingtoolreturn, Grok4.6low24turns; ownsrootmanifest/lock,Docker,CI,minimalbinaryscript. Keepdistpublicexports, brokerTSimports; no duplicateSDK. Parent correction: CI already runsall bun tests, addcorepaths/typebuildNodechecks, no duplicateCoretests.
- Docs/API polish andauthcoverage finishbriefs stillpending; Codexmigration/nativeactivity andfinalbroadergates outstanding. No commit/deploy/restart/publication.

### 2026-09-11 installation verified / documentation completion
- Grok admission/ownership final review PASS, no blockers (/tmp/core-grok-admission-final-review-report.md); prior parent164 focused tests and five lifecycle probes still current. Grok production call sites are migrated locally; running broker has not been restarted.
- Root workspace registration/lock, Docker manifest order, CI core path/type/build/Node smoke and binary frozen install landed. Parent disposable frozen install + Core typecheck/build + broker-style source import PASS (/tmp/core-clean-parent-6dkf2l1j). Root typecheck/full repository test suite running in /tmp/core-sep11-{root-types,repo-tests}.log. Host load74 may affect timeout tests.
- Grok4.6low public API docs implementation active /tmp/core-public-api-doc-fix.log exec60022, max18. Auth coverage and Codex migration remain outstanding. No commits/publication/deploy/restart.

### 2026-09-11 full repository gate and fresh root type follow-up
- Parent full repository run:3694pass3skip1fail9657assertions504files360s (/tmp/core-sep11-repo-tests.log). Only preexisting /usage live-provider timeout; bounded deterministic usage test brief prepared. Updated packed docs example passed actual Node22.
- IMPORTANT install acceptance correction: fresh Core standalone source build passes, but refreshing root node_modules exposes types:[bun-types] transitive reference; types:[bun] alone still has implicit Node type resolution errors. Parent --types bun,node plus explicit installed Node25 typeRoots PASSES. Need direct root @types/node25 + types:[bun,node], verify frozen graph; /tmp/core-workspace-types-fix-brief.txt prepared, not yet launched. No hardcoded .bun paths permitted in source.
- Docs spec reviewPASS, qualityfixes necessary: remove internal workflow narrative, honest echo.configure state, meaningful assertions, autonomous example outsideownedprompt oromit. /tmp/core-docs-polish-brief.txt prepared. Auth contracts implementer /tmp/core-auth-coverage-finish.log exec91282 active18turns.
- Parent fake native Codex probe /tmp/core-codex-autonomy-parent.ts confirms idle/0updates/already_idle while native turn running. Design /tmp/core-codex-activity-next-report.md complete; parent corrected onepointer design to perIDslotMap, bind onlystarted/ownedACK, keep unknownturnitems filtered, replay genuine autonomous turn from owned earlybuffer, preserve actual started gate. Implementation brief /tmp/core-codex-activity-implementation-brief.txt prepared36turns, not launched.

### 2026-09-11 auth contract coverage accepted
- Grok4.6low auth coverage complete without source changes; parent19auth tests61assertions pass, author49affected tests and package typecheck pass. Independent /tmp/core-auth-contract-review-report.md ACCEPT: meaningful ownership/error identity assertions, no livecredentials.
- Auth expiry remains native-agent responsibility; copiedCredentials validates/materializes JSON, preserves canonical account/logout changes, and retains failed-release lease for provider.close retry. Concurrent dirty leases may contend on canonical source lock; provider.close can require retry, does not silently overwrite winner.
- Root type fix active /tmp/core-workspace-types-fix.log exec66214, source graph verified. Next docs polish /tmp/core-docs-polish-brief.txt, then /tmp/core-usage-deterministic-brief.txt and Codex activity implementation.

### 2026-09-11 session handoff: work moved to a fresh worktree, docs + usage test accepted
- Prior Codex-driven session died on a provider error at 04:31. All uncommitted work was copied from the old worktree into `/home/ahmet/.mux/worktrees/b4d27b57-c621-4a5a-90fe-c19ffa36328d-0ba396a5/a2dac5b8-a140-4d86-b85d-3d3a46d25663` (branch mux/supermux-core-exploratio). Parent re-verified there: core 241 pass / 0 fail (15 files); Grok folder + session-manager 313 pass --isolate; root typecheck, core typecheck and build exit 0 after frozen install. Still no commits/deploy/restart/publication.
- Deterministic /usage test landed (Grok 4.6 low): optional `CommandCtx.fetchUsage` seam, default `fetchAllUsage`; tests/commands.test.ts injects a fake UsageResponse and asserts formatted Claude/Cursor text and a single fetch. Parent: 44 pass. Removes the only failing test of the 3694-test repository run.
- Docs polish landed (Grok 4.6 low, hit max turns mid-verification; parent finished): README/API.md diary stripped, `runtime.configuration()` documented optional, resume `{}` explicit-patch wording, echo driver `configure` now applies the full requested object and `configuration()` returns a copy, native `onActivity` removed from the owned prompt, `examples/custom-driver.mjs` asserts with node:assert/strict. Parent found the agent's failed-open assertion wrong: open failure + failed leftover close rejects with an `AggregateError` (`errors[0]` unsupported_operation, last = cleanup error), not a coded error. Example and both docs corrected; example passes under real Node 22.23.2 and Bun.
- Codex native activity implementation (Grok 4.6 low, 36 turns) running /tmp/core-codex-activity-impl.log, report /tmp/core-codex-activity-implementation-report.md. Owns codex/index.ts, session.ts, codex/activity/message-events tests, codex fixture.

### 2026-09-11 Codex native activity landed / parent verification
- Grok 4.6 low implemented the bounded Codex native activity contract (report /tmp/core-codex-activity-implementation-report.md): Map<turnId, slot+generation> with bounded tombstones, live turns only from turn/started or the owned ACK, setup/early buffers capped 256 with honest failure, unified replay after ACK (a real second autonomous turn survives), account/rateLimits/updated forwarded process-scoped (foreign threadId still dropped), interrupt snapshots every outstanding slot, steer/approvals require one unambiguous live slot. Session.steer: interrupting -> session_busy first, idle -> session_not_running, >1 native ids -> session_busy, owned or exactly one native activity may steer; no synthesized receipts.
- Parent probes: /tmp/core-codex-autonomy-this.ts now {state:running, updates:3, interrupt:stopped}; /tmp/core-codex-overlap-parent.ts owned hang + autonomous auto-2 -> steer "Codex turn is ambiguous", interrupt cancels both turn-1 and auto-2, activity started/completed for both.
- Parent-found regression: per-turn cancelPendingPermissions deleted the pending entry before the host abort settled, so the native approval request was never answered after interrupt/completion (old driver answered decline). Probe /tmp/core-codex-cancel-response-parent.ts: approvalAnswered 0 (new) vs 1 (old). A Grok 4.6 low fix task hit max turns after writing only the regression test; parent applied the one-line fix (abort only; settle() writes the deny exactly once), made the agent's test wait for the asynchronous deny, and dropped a duplicate. Probe now answers decline once.
- Parent hardened two implementer tests that were timing-flaky under host load ~21: native-ask test now waits for the host ask deterministically and asserts the decline via TRACE; setup-notice test polls for the post-identity chat update. Codex file 36 pass x3 alone. Full library runs: 255 pass in three of four runs; the one-off failures were these two timing tests, both now fixed; final serial reruns recorded below.
- Root typecheck, core typecheck/build and the Node 22 custom-driver example pass after these changes.
- Next: Codex broker bridge stage 1 (host + adapter + native mapper + steer-on-send + read-only rpc passthrough for skills/list), no production wiring; brief /tmp/core-codex-bridge-stage1-brief.txt, steering mailbox /tmp/core-codex-bridge-stage1-steering.md.
- Parent follow-up on the same day: a third implementer test (overlap owned+auto-2) asserted auto-2's completion before its notification could land; now polls. The native-ask test exposed a real ordering gap: a server request (approval) parsed in the same chunk as the thread/start reply was denied immediately while its turn/started still sat in the pre-identity buffer. Driver now buffers pre-identity server requests together with notices and replays them in arrival order after identity (bounded by the same 256 cap). Codex file: 36 pass in four consecutive runs; full library serial runs 255/256 pass with the only failures being the timing tests above before their fixes.
- Codex bridge stage 1 implementer (Grok 4.6 low, 48 turns) launched: /tmp/core-codex-bridge-stage1.log, report /tmp/core-codex-bridge-stage1-report.md.

### 2026-09-11 Codex bridge stage 1 landed (not yet accepted)
- Grok 4.6 low delivered src/core/agents/codex/{core-host,core-host-provider,core-adapter,native-items}.ts + tests (report /tmp/core-codex-bridge-stage1-report.md): process-level CodexCoreHost with broker policy never/danger-full-access/permissionPrompts none and env kept out of records; CoreCodexAdapter with Grok lifecycle (adopt/resume/create, epochs, close-by-id, configure rollback), idle send -> whenBusy reject, running -> Session.steer, native item mapper shared with the legacy adapter (adapter.ts now re-exports native-items.ts, behavior unchanged), image attachments as Core image blocks, rpc getter backed by a new bounded Core seam codex({ onRuntimeRequest }) that refuses turn/* and thread/* and rejects after close (documented in API.md, fixture answers skills/list). Agent: 92 pass on the isolate set, root + core typecheck clean. Parent re-ran: 92 pass, both typechecks clean.
- Parent integration probe /tmp/core-codex-bridge-parent.ts over the REAL Core Codex driver + fixture through the host: idle send -> one turn/start, steer-on-send -> turn/steer with expectedTurnId turn-1, autonomous-on-open -> turn-start/turn-complete without a send and usage forwarded, rpc skills/list works and turn/start refused. Fixture needs EXPECT_SANDBOX=danger-full-access under the broker host policy.
- Parent-found defects (fix running /tmp/core-codex-bridge-latch-fix.log, brief same basename): raw turn/completed closes the broker turn while Core is still running with overlapping turns (must latch on ordered Core state only, as the Grok bridge does); failureEmitted only resets on openTurn so consecutive idle failures surface once; idle send can hit session_busy when native work starts between snapshot and send (retry once as steer).
- Stage 2 (production wiring) still pending; note for its brief: the host must accept per-session command/args (plugin -c flags from codexSpawnArgs) and CODEX_HOME/auth env, dialect keeps auth copy, config.toml MCP shim, preamble, plugins; manager Codex kill must become adapter.stop; applyConfig via setConfiguration while idle instead of respawn.
- Latch/failure/busy-retry fix landed (Grok 4.6 low, /tmp/core-codex-bridge-latch-fix-report.md): UI turn open/close only from ordered Core stateChanged plus confirmed stop; failureEmitted resets per send/interrupt; idle send session_busy retries once as steer. Parent: bridge isolate set 97 pass; probe overlap-latch now emits only turn-start before close and turn-complete on confirmed stop. Full core 257 pass; broker agents+session-manager+tests/agents 735 pass --isolate; root typecheck clean. STAGE 1 ACCEPTED. Stage 2 (production wiring) brief being prepared.

### 2026-09-11 Codex production wiring landed / parent verification
- Grok 4.6 low (report /tmp/core-codex-production-wiring-report.md): host takes per-session command/args; dialect spawn/resume through getCodexCoreHost() with the Grok-style admission/ownership fence, auth copy + config.toml MCP shim + preamble + plugins preserved, register-before-start, pid 0, CODEX_HOME + auth env only on the driver, plugin flags without -c model, exact resume with no new thread on failed load, applyConfig via setConfiguration with typed busy; manager registers Core adapters without handle.onExit, kill awaits adapter.stop, Codex joins queue-until-idle applyOrDeferReapply (no kill+respawn), resumeCodexArm on all three resume sites; main gracefulShutdown awaits closeCodexCoreHost next to Grok. Agent: 388 pass isolate set.
- Parent: isolate set 388 pass, root typecheck + git diff --check clean. Provider failed-close retry probe attempts 2 (correct). All four manager+Core configuration probes adapted from the Grok set pass for Codex (M1/M2 serialized; M1 failure keeps newer M2; newer pending survives M1 success; M2 failure rolls native+registry+adapter back to M1).
- REAL smoke /tmp/core-codex-real-bridge-smoke-parent.ts over codex-cli 0.153.4 through CodexCoreHost with the broker command/args (sandbox forced read-only, no plugins, disposable CODEX_HOME/workdir): start 1.1s, prompt -> assistant "pong" -> turn-complete, two usage updates, skills/list ok, stop, adopt+exact resume of the same thread id, second prompt ok. No live broker restart performed; the running broker is unchanged.
- Full repository after wiring: first run 3767 pass / 3 fail — tests/spawn-pa, tests/supervisor and tests/spawn-codex-attachments still mocked the legacy codex spawn/adapter modules, so they bypassed the Core path and one of them spawned a REAL codex app-server (real thread id observed; live state dir unaffected, no stray processes). Grok 4.6 low switched them to a fake getCodexCoreHost() and pid 0 (/tmp/core-codex-root-tests-fix-report.md). Parent full repository rerun: 3770 pass, 3 skip, 0 fail across 509 files (/tmp/core-sep11-repo-tests-3.log); root typecheck + git diff --check clean. STAGE 2 ACCEPTED. Lesson: verification sets for wiring tasks must be the full `bun test --isolate`, not a folder subset.
- Remaining agents: OpenCode broker path uses `opencode serve` (HTTP) while Core speaks `opencode acp`; Claude broker path is a tmux pane + MCP shim (a product shape, not just a transport); Cursor native durable resume unverified. Read-only research task launched to size parity gaps before any further migration: /tmp/core-remaining-agents-research.log, report same basename -report.md.

### 2026-09-11 remaining agents research (read-only, Grok 4.6 low)
- Report /tmp/core-remaining-agents-research-report.md. Recommendations: Claude — do NOT migrate (broker Claude is a tmux pane + MCP shim + tailer + hooks + walkthrough + live /model; Core claude() is headless --print stream-json; a headless worker would be a new session kind, not a dialect swap). Cursor — defer (Core driver needs create-chat + store.db, unverified live; broker per-turn cursor-agent -p --resume works today). OpenCode — plausible next Core dialect but only after a live ACP proof: broker uses `opencode serve` HTTP (command.list, permission patch, session.idle), Core wraps `opencode acp` stdio; earlier live finding: ACP interrupt reported completed rather than cancelled. Cross-cutting: provider + host lifetime fence + admission fence could be extracted from Grok/Codex without behavior change (optional hygiene).
- Parent live proof of OpenCode over Core ACP launched: /tmp/core-opencode-acp-proof-parent.ts (results appended below). Final independent review of the whole migration diff launched: /tmp/core-final-review.log, report /tmp/core-final-review-report.md.
- OpenCode ACP live proof (opencode 1.16.2, /tmp/core-opencode-acp-proof-parent.ts + raw /tmp/core-opencode-acp-raw-probe.mjs): Core create/close/exact resume work (session id ses_…, resume:true, loadSession:true, sessionCapabilities close/fork/list/resume). BUT session/prompt with the user's default model (configOptions model currentValue alibaba-token-plan/qwen3.7-max) returns stopReason end_turn with usage 0/0/0 and NO agent text and NO error — a silent provider failure reported as a completed turn, so interrupt honesty could not be measured (turns finish in ~2s). ACP exposes model selection via session/new configOptions + session/set_config_option; a probe selecting opencode/big-pickle did not finish within 120s (hung or slow; killed). Conclusion: OpenCode over Core ACP is NOT ready for a broker migration without a dedicated investigation of model selection, silent-empty completions and cancel semantics; keep the HTTP `opencode serve` path. Decision recorded for the user.

### 2026-09-11 final independent review (Grok 4.6 low, read-only)
- /tmp/core-final-review-report.md: no BLOCKER. Verdict "ready to commit for human review, not to deploy" provided packages/supermux-core and bun.lock land in the same commit. Ranked: (1) host registered.clear() before adapter.stop — parent checked Core.close(): it resolves only after every live session and leftover runtime confirmed close and rejects with AggregateError otherwise, so the clear never precedes confirmed native cleanup; invariant comment added to both hosts. (2) child inheritEnv default pulls the broker process env into the app-server — identical to the legacy spawnCodexAppServer (`{...process.env, ...authEnv, CODEX_HOME}`), no new surface; Core records/logs stay secret-free. (3)-(6) nits: pending.continue() after interrupt (no-op on an empty queue), usage hook wired after start (same as legacy), legacy GrokAdapter applyConfig kept for tests, stale restart-style comment in session-types (fixed), Grok driverFactory/childFactory type shape (tests only), graphify-out/memory must not be committed.
- Remaining-agent decisions (parent): Claude stays on the tmux pane + shim (do not migrate); Cursor deferred until live create-chat + store.db are proven; OpenCode stays on `opencode serve` HTTP until ACP model selection, silent-empty completions and cancel semantics are investigated separately.
- State for review: full repository 3770 pass / 0 fail (--isolate), core 257 pass, root typecheck clean. NOTHING committed, published, deployed or restarted; the live broker still runs the pre-migration code. Next action requires the user: commit for review (exclude graphify-out/memory/ and docs/core-progress/, which is git-excluded) and, separately, a preview-broker trial before any deploy.

### 2026-09-12 committed for review
- User asked to commit the current state. Single commit on mux/supermux-core-exploratio (library + broker wiring + lockfile together; graphify-out/memory left untracked). No push, publish, deploy or restart.

## Keeper transport (detached agents) — started 2026-09-18
Design: docs/core-design/index.html (served at https://supermux-core-design.ustalabs.com). Decisions: keeper mandatory (no direct attach), one attacher at a time (new connect replaces), no defaults on the public surface, busy semantics left open until per-agent behavior is observed.
- Stage 1a (running): keeper process + client + protocol + tests, no driver change. Brief /tmp/core-keeper-stage1a-brief.txt, log /tmp/core-keeper-stage1a.log, report /tmp/core-keeper-stage1a-report.md.
- Stage 1b (next): Codex transport switches to the keeper (no direct-pipe path), fixture suite unchanged, kill-and-reattach test with cursor and parked permission.
- Then: real codex app-server go/no-go probe; defaults removal on the touched surface (close mode, resume cursor, parked deadline, Codex sandbox/approval, limits object).
- Stage 1a ACCEPTED (2026-09-18): src/keeper/{keeper,client,protocol,index}.ts, tests/keeper.test.ts (13 pass x3, no leaked keepers), echo-agent fixture, connectKeeper exported. Parent cross-process probe (scratchpad keeper-a/keeper-b): attacher exits without detach -> keeper+agent survive -> agent request parked -> new process attaches with cursor, gets the parked request, answers, sees the reply, shutdown kills both. Parent fixes after review: status writes coalesced (250 ms, immediate on key changes), journal cap hysteresis (trim to 75%), in-memory journal documented, test leak fixed, replay carries only agent->client frames (parent-applied after the fix agent hit max turns).
- Stage 1b RUNNING: Codex driver on the keeper only (ack/'acked' cursor, re-attach skips handshake via keeper meta, orphan detection, cross-process tests). Brief /tmp/core-keeper-stage1b-brief.txt.
- Stage 1b landed (Grok 4.6 low, /tmp/core-keeper-stage1b-report.md): keeper ack + 'acked' cursor, required CodexOptions.keeper, transport over connectKeeper with required close mode, re-attach skips the handshake via keeper meta.agentSessionId, orphan detection, cross-process tests. Agent: 275 core pass.
- Parent found the cursor flaw with a deterministic fixture probe and the real CLI: 'acked' replay only delivers frames the DEAD process had not handled, but the new process has fresh memory, so a turn whose turn/started was already acked is invisible (its deltas are dropped as unknown-turn items; real probe: 74 frames replayed, 0 updates, no activity). Fix (parent): the driver snapshots live turn ids into keeper meta synchronously on every bind/complete (before the frame is acked) and seeds slots from meta.liveTurns on re-attach; attacher fixture now dies only after turn/started is journaled so the test exercises replay. Fixture probe: activity turn-1 + auto-2 seeded on re-attach. REAL codex 0.153.4: attacher killed 2.5 s into a long turn; re-attach 12 ms, live turn seeded, 4015 updates and 3330 words streamed to the new process. Completion detection after re-attach being confirmed with a shorter turn.
- Design lesson for the page: "cursor" alone is not enough; a re-attaching driver needs a state snapshot (recorded by the driver in keeper meta) plus replay from the ack point. Ack must happen only after the state effects of a frame are recorded.
- GO/NO-GO PASSED and stage 1b ACCEPTED (2026-09-18): real codex 0.153.4, attacher killed 2.5 s into a 700-word turn; re-attach 69 ms, live turn seeded, 1725 updates / 1394 words streamed to the new process, completion detected at 56 s, shutdown left no process. Parent full core 275 pass, build clean, no leaked keepers. Design page section 9 records the cursor lesson.
- Next (authorized by the user's plan): defaults removal on the touched surface (Codex options required; Session/Core close with required mode; detach supported only on keeper-backed runtimes). Then STOP for review before ACP/Claude on the keeper.
- No-defaults stage 1 (Grok 4.6 low hit max turns with an empty report but most edits landed; parent finished): CodexOptions sandbox/approvalPolicy/permissionPrompts/inheritEnv/timeouts/maxFrameBytes/keeper REQUIRED (TypeError names the field); AgentRuntime.close({mode}) / Session.close({mode}) / sessions.close(id,{mode}) / core.close({agents}) REQUIRED; detach only where runtime.capabilities.detach (Codex on the keeper advertises it; ACP/Claude/Cursor reject with unsupported_operation before side effects); library-internal cleanup closes use 'shutdown' with comments; broker hosts pass explicit values (BROKER_CODEX_OPTIONS incl. timeouts + keeper limits; Grok adapter/host close modes). Parent fixes: three type-only imports of the validators, two tests calling core.close with a session-style mode, explicit 30 s budget for the real-subprocess integration test, 10 s pre-open wait in the dialect concurrency test (auth copy + home prep take ~2.5 s at load 25). Parent: core 281 pass / 0 fail, build + Node 22 example ok, root typecheck clean, broker agents+session-manager 747 pass with one failure in tests/agents/auth-contract.test.ts "cursor: the resolver is async" (untouched file, 5 s timeout under load; being checked), no leaked keepers.
- Remaining defaults (agent's list, still open): createCore interruptTimeoutMs/maxPending, ACP/Claude/Cursor option defaults, Session.interrupt pending default, create() random session id, Codex id/command/args defaults. Decision needed on grouping them into a required limits object.

## Decisions from Ahmet (2026-09-18 15:20)
1. Order: keeper transport for ACP and Claude first, then the defaults sweep.
2. Required inputs: one required `limits` object on createCore for caps/timeouts; explicit fields for the rest.
3. Normalized event contract: derive it by inspecting what each CLI actually emits (research running: /tmp/core-event-taxonomy-research.log, report /tmp/core-event-taxonomy-report.md); expect a larger set than the eight kinds proposed.
4. Busy semantics: activity carries a kind (blocking/background); decide details after observing every agent on the keeper.
5. Agent profiles: the LIBRARY owns environment (session home, credential copy, config/MCP shim, preamble, plugin flags, instructions input).
6. Claude in the broker: HEADLESS (library Claude driver), not the tmux pane. Implication recorded: the event contract and permission/question flow must cover what the pane provided (in-pane permissions, hooks, walkthrough, live /model), and the terminal display goes away.
- ACP on the keeper (Grok, OpenCode) landed (Grok 4.6 low hit max turns with an unfinished report; most edits landed; parent finished): keeper marks responses whose request id was not sent by the current attacher as stale (delivered flagged, not dropped); AcpOptions.keeper + setupTimeoutMs required; process.ts is now a keeper-backed web-stream adapter (ack after SDK consume, stale frames routed to the driver); re-attach skips initialize/session load via meta.agentSessionId and seeds meta.liveActivity; owned prompt recorded in meta.ownedPrompt so a stale prompt response ends the turn after a re-attach; detach capability true; Grok wrapper restarts sequentially (shutdown, then reopen with the same native id).
- Parent fixes: (1) client.shutdown() must wait for the keeper PROCESS to die (it returned on socket close, so a same-id reopen found the dying keeper and re-attached to the OLD agent — configure "succeeded" without restarting); (2) client waits for the new keeper's own status.json (socket appears before status) and never reuses a keeper whose agent already exited; (3) a fresh keeper renames any journal left by a dead predecessor to journal.ndjson.prev and starts at seq 1 (re-attach only ever joins a live keeper; replaying a dead process's journal into a fresh handshake mis-resolved SDK requests). Parent: keeper+agents 43 pass, core 290 pass, broker grok+codex 166 pass --isolate, root typecheck clean, no leaked keepers. Real grok go/no-go: see below.
- Event taxonomy research (decision 3) complete: /tmp/core-event-taxonomy-report.md — per-agent wire inventories from real schemas (codex app-server generate-json-schema, ACP SDK 1.4.0 schema, claude/cursor stream-json, opencode SSE) and a proposed normalized union of ~20 kinds (turn, assistant-message, reasoning, tool-call, command-output, file-diff, web-search, mcp-tool, plan/todo, task, user-question, permission-request, commands-update, mode-update, session-info, usage, compaction, warning, error) with envelope {seq, turnId, replay, origin, native}. 12 open questions listed for Ahmet.
- REAL grok GO/NO-GO PASSED (2026-09-18): grok agent stdio through the keeper; attacher killed 2.5 s into a 700-word turn; keeper meta held agentSessionId + ownedPrompt{requestId:2, activityId} + liveActivity; re-attach 55 ms, activity seeded, 2711 updates streamed, completion detected at 75 s through the stale prompt-response path, shutdown left no process. ACP-on-keeper stage ACCEPTED.
- Claude headless on the keeper landed (Grok 4.6 low; hit max turns before verification, report listed all items landed; parent verified and fixed): REQUIRED KeeperSpec.frameShape 'jsonrpc' | 'claude-control' (keeper parks/marks-stale by shape; Codex and ACP pass 'jsonrpc' explicitly); ClaudeOptions keeper/timeouts/maxFrameBytes/inheritEnv/permissionPrompts/tools required; transport over connectKeeper; re-attach skips initialize via meta.agentSessionId and seeds meta.ownedTurn as native activity, the turn's 'result' frame ends it; detach capability true; tests/claude-keeper.test.ts (process death, parked can_use_tool, orphan, replaced, detach, leak check). Parent fixes: client.shutdown() tolerates an already-destroyed socket (it threw 'keeper socket closed' as an unhandled rejection after a request-timeout failure) and every transport catches its fire-and-forget close. Parent: core 298 pass / 0 fail, build clean, no leaked keepers.
- REAL claude GO/NO-GO PASSED (2026-09-21, claude CLI headless stream-json): attacher killed 2.5 s into a 1500-word turn; keeper meta held agentSessionId + ownedTurn{uuid}; re-attach 75 ms, owned turn seeded as activity, assistant text (32k chars) and the result frame delivered to the new process, completion at 67 s, shutdown left no process. All three keeper-backed drivers (Codex, ACP/Grok, Claude) are now proven on real agents. Note: host rebooted ~2026-09-19; /tmp reports and probe scripts were lost (taxonomy report was preserved in docs/core-design/); the worktree survived but everything since c077d1d0 is still UNCOMMITTED.
- Next: defaults sweep in two slices — (1) createCore required limits object + explicit interrupt pending + explicit session id; (2) ACP/Grok/OpenCode/Cursor remaining driver options required.
- Defaults sweep slice 1 ACCEPTED (2026-09-21; Grok 4.6 low landed the edits, hit max turns before verifying; parent verified): createCore requires limits { interruptTimeoutMs, maxPending, outstandingActivity } (TypeError names the field; Session and the Core open buffer use the configured activity cap), CreateOptions.id and ForkOptions.id required (no random ids), Session.interrupt requires { pending: 'discard' | 'keep' }, broker hosts pass 10000/128/256 explicitly, docs/examples updated. Parent: core + root typecheck clean, core 303 pass / 0 fail, build + Node 22 example ok, broker grok+codex 166 pass --isolate, no leaked keepers. Remaining for slice 2: driver-internal activity caps (agents/index.ts, acp/index.ts still 256), ACP/Grok/OpenCode/Cursor/Codex optional options (command, args, id, cancel retry timings, Cursor sandbox/trust/force and timeouts).
- Briefs, logs and agent reports now live in docs/core-design/briefs/ (git-excluded docs/, survives /tmp wipes).
- Defaults sweep slice 2 ACCEPTED (2026-09-21): every driver option that used to fall back is required (Codex/Claude id+command+args; ACP args, inheritEnv, mcpServers, timeouts, maxFrameBytes, cancel retry timings, maxOutstandingActivity; Grok id/command/commandArgs/alwaysApprove/noLeader; OpenCode id/command; Cursor command/sandbox/trust/force/approveMcps/timeouts). Optional only where absence means "the flag is not sent" (model, effort, mode, permissionMode). Driver-internal activity caps come from maxOutstandingActivity. Parent re-verified: library 307 pass, broker grok+codex 166 pass, both typechecks clean, no leaked keepers.
- Full repository gate before push: 3820 pass / 3 skip / 0 fail across 513 files. Committed 55137622 and pushed branch mux/supermux-core-exploratio to origin (user authorized commits and pushes on 2026-09-21). Next: normalized event contract (needs the user's answers on the taxonomy report's open questions), then library-owned hosts/profiles/policy, then broker switch-over.

## Normalized event contract — started 2026-09-21
Decisions from Ahmet: reasoning is its own event kind, shown when the agent provides text and marked redacted otherwise; permission requests and agent questions become real UI prompts through the library.
Parent's explicit choices for the report's other open questions (veto-able): commands-update is a normalized kind; one `usage` kind with optional facets (context, tokens, cost, rateLimits); one `task` kind with taskKind; text streams as `assistant-delta` (chunk + messageId) plus a final `assistant-message` (full text + messageId), same pairing for `reasoning-delta`/`reasoning`; `tool-call` stays the universal card and `command-output`/`file-diff`/`web-search`/`mcp-tool` are additional detail kinds keyed by callId; turn boundaries come from Core state, never vendor frames; realtime/voice excluded from v1; OpenCode only via ACP in the library; every event keeps the raw native payload in the envelope; schema versions pinned in each mapper's header.
Slices: E1 types + envelope + Session wiring + Codex and ACP mappers; E1b Claude and Cursor mappers; E2 permission/question request-response API (events + respond, parked by the keeper while detached); E3 broker adapters consume the normalized stream.
- E1 ACCEPTED (2026-09-21): src/events/normalized.ts (envelope + body union), pure mappers src/codex/normalize.ts and src/acp/normalize.ts (Grok vendor unwrap), optional runtime normalize/flush, Session emits CoreEvent `session.event` with per-session monotonic seq and turn-start/turn-complete from its own state; session.update unchanged. Parent checked both mappers against the REAL wire (real codex and real grok turns through Core) and captured the frames as regression fixtures tests/fixtures/real/{codex,grok}-turn.ndjson. Real-wire defects found and fixed (Grok 4.6 low + parent finish): Codex userMessage items were emitted as tool-call and agentMessage item/started produced empty duplicate finals (now only tool-like ThreadItem types become tool-call; one assistant-message per completed item; deltas share the item id); ACP finalized reasoning only at turn end, after the answer (now a contiguous thought/text run is finalized as soon as a different content kind begins; identical consecutive commands-update deduped). Parent: library 340 pass / 0 fail, both typechecks clean, build ok, no leaked keepers.
- Seen on the real grok wire but not mapped yet (for E2): `_x.ai/session_notification:pending_interaction` (likely Grok's question/permission surface), `tool_call_delta_chunk`, `_x.ai/models/update`, `_x.ai/queue/changed`.
- E1b ACCEPTED (2026-09-21): src/claude/normalize.ts and src/cursor/normalize.ts wired into their runtimes; ClaudeOptions.partialMessages is a REQUIRED boolean (true adds --include-partial-messages). Grok 4.6 low landed the mappers and tests (hit max turns before its own verification); parent verified against the REAL wire and captured fixtures tests/fixtures/real/{claude,cursor}-turn.ndjson (hook output redacted). Real-wire findings fixed by the parent: Claude emitted two finals per text block (partial-stream accumulation + the full assistant frame) — the full frame now supersedes the partials for its own blocks; real cursor-agent emits {type:'thinking', subtype:'delta'|'completed'} frames that the taxonomy research had missed — now reasoning-delta/reasoning, finalized before the text or tool call that follows. Real event orders now: Claude turn-start, session-info, reasoning, tool-call started/completed, deltas, one assistant-message, usage, turn-complete; Cursor session-info, reasoning, message, tool-call pair, reasoning, message. Cursor needs `--model auto` on a free plan. Parent: library 361 pass / 0 fail, both typechecks clean, no leaked keepers. All four driver families now have normalized mappers checked against real captures.
- E2 groundwork (2026-09-21): parent captured what REAL agents send when they need permission (docs/core-design/captures/permission-{codex,claude,grok}.json, local only). Codex: server request item/commandExecution/requestApproval with availableDecisions accept | acceptWithExecpolicyAmendment (= allow always for this command) | cancel, plus thread/status/changed activeFlags waitingOnApproval. Grok: ACP session/request_permission with options allow-once / reject-once ("No, and tell Grok what to do differently" — a reject can carry text), plus vendor _x.ai/session_notification pending_interaction {tool_call_id, kind:'permission'} as a "waiting" marker; a denied request ends the prompt with stopReason cancelled, cancellationCategory PermissionCancelled. Claude: control_request can_use_tool with input, blocked_path and permission_suggestions (always-allow rules / add directory).
- REAL BUG fixed: with permissionPrompts 'host' the Claude driver never received can_use_tool on the real CLI (2.1.278) — Claude denied by itself (system/permission_denied) because the host route also needs `--permission-prompt-tool stdio`; the fixture had hidden it. Driver now adds the flag for 'host'; verified on the real CLI (host asked).
- E2a ACCEPTED (2026-09-21): permission requests are answerable events. CoreOptions.onPermission removed; Session implements the driver-facing requestPermission: mints a requestId, stores a PendingRequest, emits `permission-request` {requestId, toolCall{callId,tool,title,input,category}, options[{id, kind allow_once|allow_always|reject_once|reject_always, label}], detail{command,cwd,blockedPath}}, and resolves when session.requests.respond(requestId, {optionId, message?}) is called; `request-resolved` {answered|expired|cancelled} always fires; session.requests.list() returns what is pending (a request parked by the keeper while detached reappears there after re-attach); snapshot.pendingRequests. Drivers: Codex offers allow_always when availableDecisions has acceptWithExecpolicyAmendment and answers with it; Claude offers allow_always from permission_suggestions (updatedPermissions) and deny carries the message; ACP passes the agent's options through. Mappers no longer double-emit permission-request.
- Grok 4.6 low landed most of E2a but stopped with a stale report; parent finish: two option-type errors; Session.interrupt did not cancel pending requests, so a prompt blocked on a permission made interrupt come back unconfirmed (now cancelled first, request-resolved 'cancelled'); an option with an unknown kind was labelled allow_once and a request without options got invented allow/reject choices — now unclassifiable options are dropped (reject-like names map to reject_once) and a request with no usable option is cancelled with a warning, never auto-allowed; Codex approvals report tool 'execute'/'edit' instead of the raw command; detail.command falls back to rawInput.command (Grok).
- REAL verification through Core + session.requests for codex, claude and grok, allow_once and reject_once each: allow creates hello.txt, reject does not, options/detail arrive, nothing left pending. Grok ends the turn as cancelled on a reject (its own behavior). Parent: library 368 pass / 0 fail, broker grok+codex 166 pass, both typechecks clean, no leaked keepers. Next: E2b agent questions (Codex item/tool/requestUserInput, Claude AskUserQuestion, ACP elicitation), then E3 broker adapters on the normalized stream.
