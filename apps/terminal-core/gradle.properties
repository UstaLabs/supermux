# The iOS binding lives in the shared iosMain source set (iosArm64 + iosSimulatorArm64) and calls
# the st_* cinterop API; commonization makes that API visible to iosMain's metadata compilation
# (and the IDE), not just to the per-target compilations.
kotlin.mpp.enableCInteropCommonization=true
