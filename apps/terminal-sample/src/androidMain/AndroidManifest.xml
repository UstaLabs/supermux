<?xml version="1.0" encoding="utf-8"?>
<!--
  The terminal sample app. No permissions at all: it opens no socket, spawns no process and reads
  no file — every byte it shows is generated in process by SampleFixtures.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:label="terminal-sample"
        android:supportsRtl="true"
        android:theme="@style/Theme.TerminalSample">
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|density|uiMode"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
