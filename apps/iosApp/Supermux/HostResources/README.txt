Release builds stage three executable helpers into this folder before Xcode builds the app:
supermux-broker, frpc, and tmux. The binaries are generated/downloaded artifacts and are ignored
by Git; this file keeps the folder present in source checkouts.
