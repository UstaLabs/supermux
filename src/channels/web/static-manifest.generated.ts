// src/channels/web/static-manifest.generated.ts
// COMMITTED STUB — DO NOT add entries by hand.
// scripts/generate-static-manifest.ts overwrites this file during release
// builds (after the PWA is built) with one `with { type: "file" }` import per
// asset, so `bun build --compile` embeds the whole PWA. The overwrite happens
// in the CI workspace only and is never committed; source checkouts keep this
// empty map and serve the PWA from disk exactly as before.
export const EMBEDDED_STATIC: Record<string, string> = {}
